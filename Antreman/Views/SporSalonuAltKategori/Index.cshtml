@model IEnumerable<Antreman.Models.FitnessCenterSubCat>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FitnessCenter)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SubCategory)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FitnessCenter.FitnessCenterAdress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SubCategory.SubCategoryName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FitnessCenterSubCatID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FitnessCenterSubCatID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FitnessCenterSubCatID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
